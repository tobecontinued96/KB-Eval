# Copy this file to `.env` (or `.env.local`) and adjust as needed.

# Vite dev server port
DEV_PORT=9300

# Backend base URL that the frontend proxies /api requests to.
# Used by Vite dev server proxy and by api.ts via import.meta.env.VITE_API_BASE_URL.
VITE_API_BASE_URL=http://127.0.0.1:8200
